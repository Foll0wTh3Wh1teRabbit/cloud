dependencies {
    implementation project(':model')

    implementation 'org.springframework:spring-web'
}

publishing {
    publications {
        maven(MavenPublication) {
            artifact jar
        }
    }
}